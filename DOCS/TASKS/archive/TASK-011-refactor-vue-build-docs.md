# TASK-011: Сборка Vue, ассеты и документация

Дата: 2026-01-14 23:37 (UTC+3, Брест)
Статус: done
Приоритет: medium
Исполнитель: Bitrix24 Vue.js

## Цель
Стабилизировать процесс сборки Vue‑ассетов, обеспечить воспроизводимость и документировать шаги.

## Контекст
Сборка должна формировать ассеты в `public/vue/` и быть прозрачной для поддержки и деплоя.

## Требования
### Функциональные
- Описать процедуру сборки (dev/prod).
- Зафиксировать расположение ассетов и зависимость `app/index.php`.
- Обновить документацию UI‑стандартов при необходимости.

### Нефункциональные
- Воспроизводимость сборки.
- Отсутствие секретов в репозитории.

## Модули/компоненты
- `frontend/` (Vite конфигурация и исходники)
- `public/vue/` (результат сборки)
- `DOCS/GUIDES/ui-standards.md`
- `DOCS/ARCHITECTURE/tech-stack.md`

## Зависимости (иерархия)
1. TASK‑008..010.
2. `app/index.php` — подключение ассетов.

## Этапы
1. Зафиксировать команды сборки (`npm run build`, `npm run dev`).
2. Подтвердить путь выхода ассетов (`public/vue/`).
3. Обновить документацию:
   - `tech-stack.md` (версии/процесс сборки);
   - `ui-standards.md` (если меняются стили/компоненты).

## API‑методы
Не используются напрямую.

## Технические требования
- Минификация ресурсов перед деплоем.
- Проверка наличия `public/vue/app.js` и `public/vue/app.css`.

## Критерии приемки
- Сборка формирует корректные ассеты.
- Документация отражает актуальный процесс.

## Тестирование
- Локальная сборка (dev/prod).
- Проверка подстановки ассетов в `app/index.php`.

## История изменений
- 2026-01-14 23:37 (UTC+3, Брест): создана задача.
- 2026-01-15 09:39 (UTC+3, Брест): зафиксированы команды сборки и выход ассетов, подтверждена связка с `app/index.php`.