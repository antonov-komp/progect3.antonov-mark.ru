# План действий: TASK-034, TASK-035, TASK-036

**Дата:** 2026-02-12  
**Статус:** Выполнен

---

## Обзор

| Задача | Название | Статус по ТЗ | Осталось |
|--------|----------|--------------|----------|
| TASK-034 | Модуль «Пользовательские поля» | Завершена | — |
| TASK-035 | Регистрация поля из интерфейса | Завершена | — |
| TASK-036 | Поле-встройка (embed) | Завершена | — |

---

## TASK-034: Модуль «Пользовательские поля»

### Верификация критериев приёмки

| № | Критерий | Действие |
|---|----------|----------|
| 1 | Плитка «Пользовательские поля» на главной ведёт на `/user-fields` | Проверить в браузере: HomeView → AccessModuleTiles → клик по плитке |
| 2 | Страница разделов: Сделки, Лиды, Контакты, Компании, смарт-процессы | Проверить загрузку `section=sections`, отображение плиток |
| 3 | Клик по разделу → загрузка полей → таблица | Проверить переход `/user-fields/deal` и др. |
| 4 | Таблица: колонки Название, ID технический, Тип, ID записи, Обязательное, Множественное, Сортировка, В списке, Ред. в списке, В фильтре, Поиск | Просмотреть UserFieldsTable.vue — все колонки есть |
| 5 | UserFieldService покрывает все 5 категорий | ✅ Реализовано |
| 6 | API возвращает корректный JSON | Проверить ответы `section=sections` и `section=deal` |
| 7 | Ошибки API логируются, UI fallback | Проверить ErrorState при сбое Bitrix24 |
| 8 | Доступ через AccessControlService | Проверить embedded/direct режимы |

### Рекомендуемые шаги

1. **[ ]** Прогнать чек-лист тестирования из TASK-034 (§ Тестирование).
2. **[ ]** При расхождении — зафиксировать и исправить.
3. **[ ]** Отметить критерии приёмки в TASK-034 как выполненные и переместить задачу в archive.

---

## TASK-035: Регистрация поля из интерфейса

### Верификация критериев приёмки

| № | Критерий | Действие |
|---|----------|----------|
| 1 | Кнопка «Создать поле» на странице раздела | ✅ Реализовано |
| 2 | Модальное окно с формой создания | ✅ Реализовано |
| 3 | Форма: название, код (опц.), тип, обязательное, множество, в списке, в фильтре, сортировка | Проверить EDIT_IN_LIST — сейчас нет в форме |
| 4 | POST к API → Bitrix24 | ✅ Реализовано |
| 5 | Успех: закрыть модалку, обновить список, уведомление | Проверить handleFieldCreated — await loadFields() |
| 6 | Ошибка: отобразить сообщение | ✅ Реализовано |
| 7 | Поддержка всех разделов (включая smart) | ✅ Реализовано |
| 8 | Поле в Bitrix24 в форме карточки (кастомное UF_CRM_*) | Ручная проверка в CRM |

### Рекомендуемые шаги

1. **[ ]** Добавить в форму создания чекбокс «Редактировать в списке» (EDIT_IN_LIST) — опционально.
2. **[ ]** Исправить `UserFieldsView.vue`: в `handleFieldCreated` должен быть `await loadFields()`, сейчас передаётся `controller.signal` в несуществующий вызов.
3. **[ ]** Прогнать тесты из TASK-035 (§ Тестирование).
4. **[ ]** Обновить статус TASK-035 на «Завершена» и переместить в archive.

---

## TASK-036: Поле-встройка (embed)

### Фаза 2: Каталог предопределённых типов

| № | Действие | Приоритет |
|---|----------|-----------|
| 1 | **Создать** `app/config/embed-types.php` с конфигом типов `buttons_view` и `dependent_fields_view` | Высокий |

**Содержимое `app/config/embed-types.php`:**

```php
<?php
return [
    [
        'id' => 'buttons_view',
        'user_type_id' => 'buttons_view',
        'title' => 'С кнопками',
        'handler_path' => '/embed/buttons-view.php',
        'description' => 'Набор настраиваемых кнопок (звонок, email, URL)',
    ],
    [
        'id' => 'dependent_fields_view',
        'user_type_id' => 'dependent_fields_view',
        'title' => 'Зависимые поля',
        'handler_path' => '/embed/dependent-fields-view.php',
        'description' => 'Вывод полей сущности в компактном виде',
    ],
];
```

### Фаза 3: Экран «Настроить» — доработки

| № | Действие | Приоритет |
|---|----------|-----------|
| 2 | **Расширить** `isEmbedField()` в UserFieldsTable.vue — поддерживать `section=smart` | Средний |
| 3 | **Проверить** сохранение настроек для смарт-процессов: `entityIdOverride` как массив `['CRM_X', 'DYNAMIC_X']` | Средний |

### Баг: EmbedFieldSettingsService::save()

| № | Действие | Приоритет |
|---|----------|-----------|
| 4 | **Исправить** тип 5-го параметра `$entityIdOverride` в `EmbedFieldSettingsService::save()`: заменить `?string` на `array|string|null` | Высокий |

### Фаза 4: Смарт-процессы

| № | Действие | Приоритет |
|---|----------|-----------|
| 5 | **Проверить** создание поля-встройки в разделе «Смарт-процесс» — backend уже поддерживает | Высокий |
| 6 | **Проверить** отображение iframe в карточке элемента смарт-процесса | Высокий |
| 7 | **Доработать** `EmbedFieldSettingsService::save()` — корректно сохранять `by_field/{CRM_X}_{FIELD_NAME}.json` и `{DYNAMIC_X}_{FIELD_NAME}.json` для smart | Средний |
| 8 | **Проверить** handlers `buttons-view.php` и `dependent-fields-view.php` — резолв настроек по ENTITY_ID вида `CRM_23`, `DYNAMIC_23` | Средний |

### Дополнительные улучшения

| № | Действие | Приоритет |
|---|----------|-----------|
| 9 | **Исправить** UserFieldsView.vue: `handleFieldCreated` — вызов `loadFields()` должен быть `await store.loadFields(...)` с корректными аргументами | Высокий |

---

## Порядок выполнения (рекомендуемый)

### Этап 1: Критические исправления (1–2 часа)

1. Создать `app/config/embed-types.php`.
2. Исправить тип `$entityIdOverride` в `EmbedFieldSettingsService::save()`.
3. Исправить `handleFieldCreated` в UserFieldsView.vue.

### Этап 2: TASK-034 — верификация (0.5–1 час)

4. Прогнать чек-лист тестирования.
5. Отметить критерии приёмки.
6. Переместить TASK-034 в archive (если ещё не перенесена).

### Этап 3: TASK-035 — верификация и закрытие (0.5–1 час)

7. Добавить EDIT_IN_LIST в форму (опционально).
8. Прогнать тесты создания поля.
9. Обновить статус и переместить в archive.

### Этап 4: TASK-036 — фаза 4 и доработки (1–2 часа)

10. Расширить `isEmbedField()` для `section=smart`.
11. Проверить создание embed-поля в смарт-процессе.
12. Проверить сохранение настроек для smart (by_field).
13. Обновить TASK-036: отметить фазу 4 выполненной.

### Этап 5: Документация и финализация

14. Обновить историю правок в TASK-034, TASK-035, TASK-036.
15. Зафиксировать итог в README TASKS или release notes.

---

## Чек-лист перед закрытием задач

- [x] Все критерии приёмки TASK-034 отмечены как выполненные
- [x] Все критерии приёмки TASK-035 отмечены как выполненные
- [x] Фазы 1–4 TASK-036 отмечены как выполненные
- [x] config/embed-types.php создан и подключён
- [x] EmbedFieldSettingsService исправлен (тип entityIdOverride)
- [x] handleFieldCreated корректно обновляет список полей
- [ ] Ручное тестирование: создание полей (standard + embed) во всех разделах
- [ ] Ручное тестирование: настройка embed-полей (buttons_view, dependent_fields_view)
- [ ] Проверка iframe в карточках deal, lead, contact, company, smart

---

## История правок

- 2026-02-12: Создан план на основе анализа кодовой базы и содержания TASK-034, TASK-035, TASK-036.
- 2026-02-12: Выполнены этапы 1–4: создан embed-types.php, исправлен EmbedFieldSettingsService, handleFieldCreated, добавлен EDIT_IN_LIST, расширен isEmbedField для smart; обновлены TASK-034, TASK-035, TASK-036.
